# 모델을 공유하는 코덱의 총 전송량 — 측정 보고

> **이건 방법론 논문이 아니라 분석 보고다.** 여기 쓰는 회계(모델+잔차의 총
> 기술 길이)는 MDL/Kolmogorov 의 표준이고, 학습 텍스트 압축(L3TC)에서 이미
> 쓰이며, 오디오 코덱 논문들도 모델 크기를 보고한다. "우리가 제안한다" 고
> 쓸 수 없다 → [선행 연구](docs/prior-art.md)
>
> 남는 기여는 **그 회계를 손실 오디오 코덱에 적용해 실제로 재 보고, 그 과정에서
> 내 자신의 초기 결론 두 개가 틀렸음을 밝힌 것** 이다. 아래에 그 정정을 남겨 둔다.
>
> 수치는 전부 [docs/](docs/) 의 실험에서 나온 실측이고 재현 스크립트는 `src/` 에 있다.

## 한 문장

**신경망 코덱은 압축률로 평가되지만 실제로 옮겨야 하는 것은 `모델 + 압축된 내용`
이다. 그 단위로 다시 재면 어느 코덱을 쓸지가 달라질 수 있다 — 그리고 압축률
자체도 비교 기준을 어디에 두느냐에 따라 3배 넘게 흔들린다.**

## 쓰는 지표

```
총전송량(N) = 모델크기 + N × 편당전송량
손익분기 N* = 모델크기 / (기준코덱_편당 − 신경망_편당)
```

`N*` 를 시간 단위로 내면 해석이 된다 — "몇 시간 분량부터 이득인가".

모델 크기는 **수신 측이 실제로 받아야 하는 것** 으로 센다:
인코더 제외(단방향 전송), 학습용 EMA 버퍼 제외, dtype 은 배포 시나리오별로.

## 결과

### 1. 신경망 오디오 코덱의 실제 이득은 5~10배 대다

48 kHz 스테레오 음악 7곡, 같은 품질(mel) Opus 대비 최대 절약:

| | EnCodec48 | DAC44 |
|---|---:|---:|
| 중앙값 | 5.7x | 6.6x |
| 범위 | 4.5 ~ 10.4x | 3.1 ~ 13.4x |

### 2. 내 초기 숫자(44배)는 Opus 를 설계 범위 밖에서 돌린 결과였다

24 kHz 모노 실험에서 Opus 를 **6~7 kbps** 까지 내려 돌렸다. 그 지점의 Opus 는
mel 1.5 — 음악으로 알아듣기 힘든 수준이고, Opus 의 음악 동작 범위 밖이다.
거기서 Opus 가 무너지니 신경망 코덱의 배수가 커 보였다.

같은 곡을 48 kHz 스테레오에서 제대로 된 Opus 최저치(16 kbps)와 견주면:

| | 내가 쓴 Opus 최저 | 그 품질 | EnCodec 절약 |
|---|---|---|---|
| 실험 8 (24 kHz 모노) | 7.0 kbps | mel 1.517 | **21.7x** |
| 실험 9 (48 kHz 스테레오) | 18.9 kbps | mel 0.406 | **5.7x** |

코덱도 곡도 같다. 바뀐 건 내 비교 기준뿐이다.
→ [실험 9](docs/results-09-music-48k.md)

### 3. "순위 역전" 은 일반 법칙이 아니다

24 kHz 판(EnCodec/DAC/Mimi)에서는 압축률 순위와 손익분기 순위가 **20/20 칸**
반대였다. 나는 이걸 이 프로젝트의 핵심 발견으로 적었다.

48 kHz 판(EnCodec48/DAC44)에서는 **3/7 칸** 만 역전한다. 24 kHz 판에서는
*압축률 1등(Mimi)* 과 *모델 최소(EnCodec)* 가 다른 모델이었지만, 48 kHz 판에서는
EnCodec48 이 둘 다인 칸이 많기 때문이다.

**역전은 회계의 필연이 아니라 그 코덱 조합의 성질이다.** 살아남는 주장은 더
약하고 더 정확한 쪽이다:

> 압축률 순위와 총 전송량 순위는 **다를 수 있다**. 그래서 압축률만 보고하는
> 표로는 어느 코덱을 쓸지 결정할 수 없다.

### 4. 손익분기는 배포 조건에 크게 좌우된다

같은 측정에서 배포 방식만 바꿔도:

| | 24 kHz 모노 (5클립) | 48 kHz 스테레오 음악 (7곡) |
|---|---|---|
| 절약 배수 중앙값 | 18.4x | **6.1x** |
| 손익분기 중앙값 (디코더 fp16) | 11.5h | **3.7h** |
| 손익분기 전체 범위 | 0.8h ~ 171.3h (206배) | 0.3h ~ 31.0h (103배) |

배수는 작아졌는데 손익분기는 빨라졌다 — 48 kHz 스테레오는 절대 비트레이트가
커서 초당 절약량이 크기 때문이다. **"몇 시간부터 이득인가" 에 단일 숫자로
답할 수 없다.**

### 5. 내용이 코덱보다 중요하다

같은 "음악" 안에서 EnCodec 이 3.6배와 21.7배로 6배 갈린다 (24 kHz 판).
축은 장르가 아니라 **고역 에너지**다. 3~8 kHz 에 23% 인 곡과 8% 인 곡의 차이다.
→ [실험 8](docs/results-08-real-music.md)

### 6. int8 디코더는 쓸 수 있다

수신 측(디코더 + 코드북)을 int8 로 눌러도 mel 저하는 최악 +0.0285,
코덱 자체 손실(0.26~1.02)의 11% 미만이다. 모델 비용이 4배 줄어든다.

단 이 측정은 세 번이나 **양자화가 실제로 안 먹은 채 그럴듯한 숫자**를 냈다
(EnCodec 디코더 43%, DAC 코드북 0%, 다시 32%). 커버리지를 분해표와 대조해서야
드러났다. → [실험 7](docs/results-07-int8-decoder.md)

### 7. 지표를 바꾸면 순위가 바뀐다

`pop` 에서 EnCodec48 은 mel 기준 10.2배, msSTFT 기준 4.9배다. `piano` 의 DAC 는
3.9배 대 7.9배로 방향이 반대다. 24 kHz 판에서도 5클립 중 3클립에서 순위가 달랐다.

**청취 평가 없이 단일 스펙트럼 지표로 코덱 순위를 매기는 건 신뢰할 수 없다.**
이 문서의 모든 배수에 그 불확실성이 붙어 있다.

### 8. 영상은 손익분기가 없다

우리가 시험한 순진한 VAE-latent 코딩은 같은 품질에 **더 많은** 비트를 썼다.
편당 절약이 음수이므로 손익분기가 존재하지 않는다. 335 MB 를 보내고도 계속 손해다.

(범위 주의: 순진한 latent 코딩에 대한 것이다. DCVC-RT 등 현재의 신경망 영상
코덱은 H.266 대비 21% 를 아낀다. 그 손익분기는 **아직 안 계산했다.**)

## 함의

**모델 크기가 그 코덱이 살 수 있는 배치 영역을 정한다.**

- **OS·드라이버·브라우저 내장** → 상각되어 무시 가능. 압축률이 곧 성능.
  DLSS 가 이 경우다 — 71블록 신경망이 드라이버에 한 번 실려 모든 게임의 모든
  프레임에 쓰인다. 손익분기가 사실상 0 이다.
- **스트리밍 서비스** → 사용자당 1회. EnCodec48 디코더 fp16(19 MB)이면 음악
  1~4시간이면 회수.
- **일회성 전송** → 매번 전액. 짧은 클립이면 신경망 코덱이 진다.

신경망 코덱이 어려운 건 모델이 커서가 아니라 **양쪽이 따로 받아야 하고 코덱마다
다른 모델이 필요해서**다. 지금 Opus 가 있는 자리(OS 기본 탑재)에 신경망 디코더가
하나 들어가는 순간 이 표의 모든 칸이 0 이 된다.

## 실험 목록

| | 결과 |
|---|---|
| [1. 영상 VAE latent](docs/results-01-vae-latent.md) | x264 에게도 진다 |
| [2. 오디오 EnCodec](docs/results-02-audio.md) | 이긴다 (**나중에 실험 9 가 배수를 정정**) |
| [3. MIDI](docs/results-03-midi.md) | 89배, 오차 0. 단 사운드폰트가 MIDI 의 2,018배 |
| [4. 2026 코덱들](docs/results-04-sota.md) | DAC 이 전 구간에서 Opus 를 이긴다 |
| [5. 손익분기](docs/results-05-breakeven.md) | 순위 역전 발견 |
| [6. 디코더만 세기](docs/results-06-decoder-breakeven.md) | 역전 유지, 범위 61배 |
| [7. int8 실측](docs/results-07-int8-decoder.md) | 저하 11% 미만. **양자화가 3번 안 먹었다** |
| [8. 진짜 음악](docs/results-08-real-music.md) | 곡에 따라 6배 갈린다 |
| [9. 48 kHz 스테레오](docs/results-09-music-48k.md) | **배수 정정, 역전은 일반 법칙 아님** |
| [10. 지표를 심판대에](docs/results-10-perceptual.md) | **앵커가 내 msSTFT 를 즉사시켰다.** 사람 기준 최종 5.1x / 3.3x + 청취 시험 페이지 |

## 아직 하지 않은 것

- [ ] **청취 평가.** 시험은 만들어 뒀다 —
      [귀로 다시 재기](https://claude.ai/code/artifact/85074fb5-1c44-48c3-a296-7f21a04385ca)
      (MUSHRA, 숨은 원본 + 3.5 kHz 앵커, 5곡). **사람이 아직 안 돌렸다.**
- [ ] L3TC 와 Practical Kolmogorov optimality 원문 확보해 대조
- [ ] DCVC 계열 영상 코덱으로 같은 계산
- [ ] 여러 작업을 공유하는 모델의 비용 배분을 어떻게 볼 것인가
      (DLSS 처럼 한 모델이 무한히 많은 작업에 쓰이면 배분을 어떻게 하나)
- [ ] 곡 수 확대. 지금 7곡이고 클래식 오케스트라·메탈·EDM 이 없다
- [ ] `mixed` 클립 교체. 에너지의 82%가 200 Hz 미만이라 이름값을 못 한다
- [ ] Opus 격자 세분화. 지금 배수의 분해능이 ±1 단계다

## 정정 기록

이 프로젝트에서 내가 틀렸다가 측정으로 바로잡은 것들:

1. "코덱 논문은 모델 크기를 보고하지 않는다" → 보고한다 (DualCodec, TS3-Codec)
2. "이 회계는 새롭다" → MDL/Kolmogorov 의 표준이고 L3TC 가 이미 쓴다
3. "고비트레이트에서는 Opus 가 이긴다" → DAC 이 반박 (실험 4)
4. 무음 클립(RMS 0.000000)을 음악으로 쓰고 msSTFT 수백만을 못 알아챔 (실험 2)
5. 체크포인트 크기가 파라미터 수와 안 맞는 걸 설명 못 함 → RVQ 코드북이
   파라미터가 아니라 버퍼였다 (실험 6)
6. Mimi 의 `downsample` 을 수신 필요분에 넣음 → 인코더 쪽이다 (실험 7)
7. int8 열을 안 재고 표에 넣음 → 쟀더니 맞았지만, 세 번이나 양자화가 안 먹은
   채 그럴듯한 숫자가 나왔다 (실험 7)
8. **"오디오는 44배 이긴다" → Opus 를 설계 범위 밖에서 돌린 결과. 실제 5~10배** (실험 9)
9. **"순위 역전 20/20" → 그 코덱 조합의 성질이지 법칙이 아니다. 48 kHz 판은 3/7** (실험 9)
