# 초안 — 모델을 공유하는 코덱의 총 전송량 회계

> 작성 중인 초안이다. 아직 논문이 아니다.
> 수치는 [docs/](docs/) 의 실험에서 나온 실측이고, 재현 스크립트는 `src/` 에 있다.
>
> **⚠ 선행 연구 조사 결과 이 초안의 전제가 여러 개 틀렸다.**
> 총전송량 회계는 MDL/Kolmogorov 의 표준이고, 학습 텍스트 압축(L3TC)에서
> 이미 쓰이며, 오디오 코덱 논문들도 모델 크기를 보고한다.
> **"우리가 제안한다" 고 쓸 수 없다.** → [선행 연구](docs/prior-art.md)
>
> 남는 기여는 **알려진 회계를 손실 오디오 코덱에 적용해 순위 역전을
> 실측한 것** 정도다. 방법론 논문이 아니라 분석 보고로 써야 한다.
> 그리고 **수신 측은 디코더만 필요하므로 수치를 다시 계산해야 한다.**

## 한 문장

**신경망 코덱은 압축률로 평가되지만, 실제로 옮겨야 하는 것은
`모델 + 압축된 내용` 이다. 그 단위로 다시 재면 순위가 뒤집힌다.**

## 문제

신경망 코덱 논문은 rate-distortion 곡선을 보고한다. 비트레이트와 품질.
그 표에 **디코더 크기는 없다.**

그런데 이 코덱들은 송·수신 양쪽이 같은 모델을 갖고 있어야 동작한다.
385 MB 짜리 모델을 쓰는 코덱으로 1시간짜리 팟캐스트를 보내면,
실제로 옮긴 것은 385 MB + 0.2 MB 다. Opus 로 보냈으면 10 MB 였다.

**압축했는데 38배를 더 보냈다.**

## 쓰는 지표 (MDL 의 특수 경우)

```
총전송량(N) = 모델크기 + N × 편당전송량
손익분기 N* = 모델크기 / (기준코덱_편당 − 신경망_편당)
```

`N*` 를 **시간 단위**로 내면 해석이 된다 — "몇 시간 분량부터 이득인가".

## 측정 결과

Opus 대비 같은 품질(mel 거리) 기준, 실제 체크포인트 크기 사용:

| 코덱 | 모델 | 압축률 | 손익분기 |
|---|---:|---:|---:|
| Mimi | 385 MB | **44×** | 40 시간 |
| DAC | 299 MB | 29× | 16 시간 |
| EnCodec | 93 MB | 15× | **10 시간** |

**두 순위가 정확히 반대다.**

압축률이 가장 높은 코덱이 총 전송량 기준으로는 가장 늦게 본전이 된다.
모델이 크기 때문이다.

## 함의

**모델 크기가 그 코덱이 살 수 있는 배치 영역을 정한다.**

- OS·앱 내장 → 상각되어 무시 가능. 압축률이 곧 성능
- 스트리밍 서비스 → 사용자당 1회. 며칠이면 회수
- **일회성 전송** → 매번 전액. **신경망 코덱이 항상 진다**

압축률만 보고하면 마지막 경우를 판단할 수 없다.

## 영상에서의 극단

우리가 시험한 순진한 VAE-latent 코딩은 같은 품질에 **더 많은** 비트를 썼다.
편당 절약이 음수이므로 **손익분기가 존재하지 않는다.** 335 MB 를 보내고도
그 뒤로 계속 손해다.

(범위 주의: 이는 순진한 latent 코딩에 대한 것이고, DCVC-RT 등 현재의
신경망 영상 코덱은 H.266 대비 21% 를 아낀다. 그 경우의 손익분기는
따로 계산해야 한다 — **아직 안 했다.**)

## 아직 하지 않은 것

- [x] **선행 연구 조사** — 했다. 회계는 이미 표준이었다. → [prior-art.md](docs/prior-art.md)
- [ ] **디코더만으로 손익분기 재계산.** 단방향 전송에서는 인코더가 필요 없다.
      비대칭 코덱(XCodec2 등)은 수신 측 손익분기가 훨씬 빠르다
- [ ] L3TC 와 Practical Kolmogorov optimality 원문 확보해 대조
- [ ] 모델 양자화 후 손익분기 (INT8 이면 4배 준다)
- [ ] DCVC 계열 영상 코덱으로 같은 계산
- [ ] 여러 작업을 공유하는 모델의 비용 배분을 어떻게 볼 것인가
- [ ] 청취·시청 평가. 지금은 mel 거리 하나로 품질을 맞췄다
- [ ] 진짜 음악
